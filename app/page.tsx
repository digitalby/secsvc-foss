export default function Home() {
  return (
    <main style={{ fontFamily: "monospace", padding: "2rem" }}>
      <h1>secsvc-foss</h1>
      <p>1Password Connect-compatible proxy. Service is running.</p>
      <p>
        API endpoint: <code>/v1/vaults</code>
      </p>
    </main>
  );
}
