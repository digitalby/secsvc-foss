# Real 1Password service account token (ops_...) — never sent to clients
OP_SERVICE_ACCOUNT_TOKEN=

# Bearer token the Android app sends as its "Service Account Token"
API_ACCESS_TOKEN=

# Optional: restrict all requests to a single vault ID
ALLOWED_VAULT_ID=
